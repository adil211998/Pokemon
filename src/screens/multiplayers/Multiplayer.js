
import React from "react";
import MultiplayerView from "./MultiplayerView";

const Multiplayer = ({ navigation }) => {
  return <MultiplayerView navigation={navigation} />;
};

export default Multiplayer;
