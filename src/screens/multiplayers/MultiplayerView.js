import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const MultiplayerView = ({ navigation }) => {
  return (
    <SafeAreaView>
      <Text>MultiplayerView</Text>
    </SafeAreaView>
  );
};

export default MultiplayerView;

